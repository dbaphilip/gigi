<div class="row brico fs-4">
  <div class="col-8">
    <h1 class="fw-bold mb-3"> User feed </h1>
  </div>
  <div class="col-4">
    <aside class="shadow-secondary">
      <table class="table">
        <thead class="text-center">
          <tr>
            <th scope="col"> <%= @user.name %> </th>
          </tr>
          <tr>
            <th scope="col" class="text-muted"> <%= @user.phone %> </th>
          </tr>
        </thead>
        <tbody class="text-center">
          <tr>
            <td colspan="2">
              <%= link_to "Edit my account", edit_user_path(@user), class: "shadow-tetiary btn btn-primary btn-lg" %>
            </td>
          </tr>
          <tr>
            <td colspan="2">
              <%= link_to "Delete my account", delete_user_path(@user), class: "shadow-tetiary btn btn-danger btn-lg" %>
            </td>
          </tr>
        </tbody>
      </table>
    </aside>
  </div>
</div>